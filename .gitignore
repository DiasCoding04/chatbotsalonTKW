# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local
.env.*.local
data/facebook-token-vault.json

# Bản dump debug webhook (PSID) — tạo khi FACEBOOK_WEBHOOK_LOG_RAW_BODY=1
data/facebook-webhook-last.json

# Release archives — GitHub không cho push file > 100MB, build local rồi upload thẳng.
release/

# Google Cloud SDK local config/credentials (không đưa lên GitHub)
.gcloud/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
